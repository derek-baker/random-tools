DEPENDENCIES:
    NodeJS/NPM
    Admin privs
    Access to an SMTP


GETTING STARTED:
    cd ./Cypress && npm install
    To run Cypress interactively: 'npm run cypress:open'
        (see package.json for details)
    To run Cypress non-interactively: 'npm run cypress:run'
        (see package.json for details)