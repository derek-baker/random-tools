// export const LoginUserWithPassword = (): void => {
//     const userName = '<TODO>';
//         const password = '<TODO>';
//         cy.get('<TODO>').type(userName).should('have.value', userName);
//         cy.get('<TODO>').type(password).should('have.value', password);
//         cy.get('<TODO>').click();
// };